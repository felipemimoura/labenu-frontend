import React from 'react'

const Input = ({ label, type, name, value, onChange, error, onBlur }) => {
  return (
    <div>
      <label>
        {label}
      </label>

    </div>
  )
}

export default Input
